import Flutter
import UIKit
import XCTest

// Add unit tests for the SipConnect plugin here.
class RunnerTests: XCTestCase {
  func testExample() {
    // Native plugin classes are exercised via the example app and Dart tests.
  }
}
